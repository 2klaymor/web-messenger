@import '../styles/variables';

.contact {
  display: flex;
  flex-direction: row;
  align-items: center;
  width: 100%;
  border: none;

  img {
    height: 2.5rem;
    border-radius: 50%;
  }
}

.contact-info {
  display: flex;
  flex-direction: column;
  align-items: flex-start;
  padding-left: 0.5rem;
  text-wrap: nowrap;
}

