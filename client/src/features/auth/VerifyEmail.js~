import {Link} from "react-router-dom";

const VerifyEmail = () => {
    return (
        <div>
            <h1>подтверждение электронной почты</h1>
            <p>мы отправили 6-значный код на ваш email</p>
            <p>пожалуйста, введите его ниже, чтобы завершить регистрацию</p>
            <p>это нужно, чтобы убедиться, что вы действительно являетесь владельцем этой почты</p>

            <div>
                {[...Array(6)].map((_, i) => (
                    <input key={i} type="text" maxLength="1" />
                ))}
            </div>

            <p>не получили код?
                <Link to="#">отправить повторно</Link>
            </p>

            <p>указали неверную почту?
                <Link to="/signup">изменить e-mail</Link>
            </p>

        </div>
    )
}

export default VerifyEmail;