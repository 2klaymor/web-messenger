@import '../../styles/variables';

.contacts {
  height: calc(100vh - 85px);
  border: 1px solid red;
}

.chat {
  display: flex;
  flex-direction: column;
  width: calc(100vw - 300px);
  //border: 1px solid cyan;
}

.chat-info {
  text-align: center;
  font-size: $font-size-s;
  //border: 1px solid blue;
}

.online-status {
  font-size: $font-size-xs;
}

.chat-history {
  flex: 1;
  font-size: $font-size-s;
  background-color: $tertiary;
  //border: 1px solid cornflowerblue;
}

.chat-input {
  display: flex;
  flex-direction: row;
  padding: 10px;
  margin-top: auto;
  align-items: baseline;
  font-size: $font-size-s;
  //border: transparent transparent $primary-text $primary-text;
}
.chat-input .btn {
  margin-left: -50px;
  border: none;
}

.chat-input img {
  height: 2em;
}

.chat-input textarea {
  display: block;
  margin-left: 50px;
  margin-right: 50px;

  max-height: 300px;
  width: calc(100% - 100px);

  resize: none;

  font-size: $font-size-xs;
  background-color: $primary-bg;
  color: $primary-text;


  &::-webkit-scrollbar {
    width: 5px; // ширина скроллбара
    background-color: $primary-bg // цвет фона скроллбара
  }

  &::-webkit-scrollbar-thumb {
    background-color: $secondary;// цвет скроллбара
    border-radius: 5px; // радиус скругления углов скроллбара
  }

  &::-webkit-scrollbar-thumb:hover {
    background-color: #888; // цвет скроллбара при наведении курсора
  }
}